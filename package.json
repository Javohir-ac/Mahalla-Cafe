{
  "name": "mahalla-cafe",
  "version": "1.0.0",
  "private": true,
  "description": "Mahalla Cafe - Restaurant Management System",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "postinstall": "echo '✅ Barcha dependencylar o‘rnatildi! Frontend uchun → cd frontend && npm start | Backend uchun → cd backend && npm run dev'"
  }
}
